{
  "name": "sennacar",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build successful' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}